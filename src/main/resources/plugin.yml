name: ${name}
main: ${package}.${name}
version: ${version}
authors: [DeviousCraker]

commands:
   hqmdatareset:
      description: Manage

permissions:
    hqmdatareset.manage:
        description: Manage
        default: op
